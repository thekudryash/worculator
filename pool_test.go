package main

import (
	"testing"
)

func TestPool_calculate(t *testing.T) {
	type fields struct {
	}
	type want struct {
	}
	type test struct {
		name string
		fields
		want
	}

	tests := []test{}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
		})
	}
}
